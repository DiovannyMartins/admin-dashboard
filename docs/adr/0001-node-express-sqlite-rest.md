# Node + Express + SQLite com REST, sem auth inicial

Back-end em `backend/` (porta 3001, CORS liberado) com Node + Express + SQLite, expondo `/api/users`, `/api/stats`, `/api/performance/weekly` e `/api/notifications`. Sem autenticação na primeira versão para focar no CRUD com troca total do localStorage por ApiService via fetch.
